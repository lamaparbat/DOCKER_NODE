version: '3'
services:
  first-container:
    build: .
    ports:
     - '3000:3000'
    volumes:
     - ./:/app
     - /app/node_modules
    environment:
     - PORT=3000
    env_files:
     - ./.env
    command: node index.js
    